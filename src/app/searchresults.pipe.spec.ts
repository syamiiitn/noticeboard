import { SearchresultsPipe } from './searchresults.pipe';

describe('SearchresultsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchresultsPipe();
    expect(pipe).toBeTruthy();
  });
});
